#get namespace;		// specify thr namespace

namespace pedro
{
	public class Class1
	{
		public void Main()
		{
			// here, is everything runs!
			cout "what the heck?";

			// uses variables inside of the string
			// realize these unassigned variables will be null if not assigned.
			string pedro;	// you can use unnasigned variables.
			cout p"pedro is like: { pedro }";	// uses the 'pedro' variable inside of the string
		}
	}
}